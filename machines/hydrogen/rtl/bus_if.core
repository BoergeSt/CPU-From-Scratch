CAPI=2:
name: :hydrogen:bus_if
description: Hydrogen machine bus protocol interface (see machines/hydrogen/docs/bus.md)

filesets:
  rtl:
    files:
      - bus_if.sv
    file_type: systemVerilogSource

targets:
  # No toplevel/flow -- not directly runnable, only a dependency target.
  default:
    filesets: [rtl]
