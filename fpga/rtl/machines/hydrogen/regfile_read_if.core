CAPI=2:
name: :hydrogen:regfile_read_if
description: Hydrogen machine register-file read port interface (see docs/machines/hydrogen/regfile.md)

filesets:
  rtl:
    files:
      - regfile_read_if.sv
    file_type: systemVerilogSource

targets:
  # No toplevel/flow -- not directly runnable, only a dependency target.
  default:
    filesets: [rtl]
