CAPI=2:
name: :hydrogen:flow_ctl_if
description: Hydrogen machine flow-control/requester interface (see docs/machines/hydrogen/flow_ctl.md)

filesets:
  rtl:
    files:
      - flow_ctl_if.sv
    file_type: systemVerilogSource

targets:
  # No toplevel/flow -- not directly runnable, only a dependency target.
  default:
    filesets: [rtl]
