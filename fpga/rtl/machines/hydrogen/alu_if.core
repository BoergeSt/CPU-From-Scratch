CAPI=2:
name: :hydrogen:alu_if
description: Hydrogen machine ALU/requester interface (see docs/machines/hydrogen/alu.md)

filesets:
  rtl:
    files:
      - alu_if.sv
    file_type: systemVerilogSource

targets:
  # No toplevel/flow -- not directly runnable, only a dependency target.
  default:
    filesets: [rtl]
